<!DOCTYPE html>
<html>
<head>
  <title>Simple, Purposeful Comparisons</title>
  <link rel="shortcut icon" href="/favicon.ico" >
  <%= render 'layouts/js' %>
  <%= csrf_meta_tag %>
  <%= render 'layouts/stylesheets' %>
</head>
<body>
  <div class="container round shadow">
	<section class="round">
      <%= render 'layouts/header' %>
	  <% flash.each do |key, value| %>
	    <div class="flash <%= key %> round_top"><%= value %></div>
	  <% end %>
	  <%= yield %>
	</section>
  </div>
  <%= debug(params) if Rails.env.development? %>
</body>
</html>
